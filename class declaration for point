class Point{
private
int x;
private
int y;
public void
setX ( int x){
x= (x > 79 ? 79 : (x < 0 ? 0 :x));
}
public void
setY int y){
y= (y > 24 ? 24 : (y < 0 ? 0 : y));
}
public
int getX (
return x;
}
public
int getY (
return y;
}
}
